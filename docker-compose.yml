services:
  api:
    build: .
    container_name: node-sqlite-api
    ports:
      - "4000:4000"
    volumes:
      - .:/app
      - node_modules_cache:/app/node_modules

volumes:
  node_modules_cache:
